import { LockScreenDialogComponent } from './lock-screen-dialog.component';
import { storiesOf } from '@storybook/angular';

storiesOf('LockScreenDialogComponent', module).add('LockScreenDialogComponent', () => ({
  component: LockScreenDialogComponent,
  template: '',
}));
