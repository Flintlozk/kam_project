import { cmsEnvironmentServices } from './cms-environment-services';

describe('cmsEnvironmentServices', () => {
  it('should work', () => {
    expect(cmsEnvironmentServices()).toEqual('cms-environment-services');
  });
});
